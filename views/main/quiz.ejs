<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/quiz.css' />
    <title> <%= title %></title>
  </head>
  <body>
    <%-include("../header.ejs")%>
    <%-include("../background.ejs")%>

    <div class="quiz-frame pixelify-sans" id="quiz-frame-id">
      <div class="quiz-wrapper pixelify-sans">
        <div class="quiz-wrapper-top">
          <h1>Question <%= questionNumber %> of <%= questions %></h1>
          <h3><%= question.question %></h3>
        </div>

        <div class="quiz-wrapper-bottom">
          <form id="answer-form" action="/play" method="POST">
            <input id="total-qs" name="questions" value="<%= questions %>" hidden readonly>
            <input name="score" value="<%= score %>" hidden readonly>
            <input id="timer" name="timer" value="<%= timer %>" hidden readonly>
            <input id="currentTime" name="currentTime" value="<%= currentTime %>" hidden readonly>
            <input id="current-qs" name="currentQuestion" value="<%= questionNumber %>" hidden readonly>
            
            <div class="options-frame">
              <% options.forEach(function(option) { %>
                <div class="option no-show" id="<%= option.key %>">
                  <input id="<%= option.key %>"class="answerChoice" type="radio" name="answer" value="<%= option.value %>" required>
                  <div class="label-wrapper">
                    <label for="<%= option.key %>"><%= option.key %>: <%= option.value %></label>
                  </div>
                </div>
              <% }); %>
              <input id="ansk" name="ansk" value="<%= answer %>" hidden readonly>
            </div>
          </form>

          <button form="answer-form" type="submit" id="submit-answer" class="btn pixelify-sans no-show">Submit Answer</button>
        </div>

      </div>
    </div>

    <%-include("../footer.ejs")%>
    <script src="./javascripts/quiz.js" defer></script>
  </body>
</html>